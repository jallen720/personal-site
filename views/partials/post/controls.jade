button(
  class='btn left-btn'
  ng-click='postAction("Read", post._id)'
) Read

button(
  ng-show='isLoggedIn()'
  class='btn left-btn'
  ng-click='postAction("Update", post._id)'
) Update

button(
  ng-show='isLoggedIn()'
  class='btn left-btn'
  ng-click='postAction("Delete", post._id)'
) Delete
